Set up of the DB

- 1. Run the create_tables.sql script which should create all of the tables for the DB structure. 

- 1. Run the foreign_key_relationship.sql script which will create all of the foreign key relationships between the tables.

- 1. Run the insert_values.sql script to insert all of the enum values into the DB. 